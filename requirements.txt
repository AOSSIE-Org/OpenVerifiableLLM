torch>=2.10.0,<3.0
numpy>=2.4,<3.0
tqdm>=4.67,<5.0
safetensors>=0.4.5,<1.0
pynacl>=1.5,<2.0          # ed25519 signing (verify-before-load security fix)
matplotlib>=3.7,<4.0     # divergence-accumulation plot (T7); optional
model-signing>=1.1.1      # Sigstore/model-transparency signing + verification CLI
huggingface_hub>=0.24.0   # fetch/publish Hugging Face model repositories
# wikitext dataset option additionally needs: pip install datasets

# This default torch wheel is CPU-only (or CUDA, depending on your platform).
# For an Intel GPU (Iris Xe / Arc), install the XPU build instead — see README
# "Locally on an Intel GPU":
#   pip install torch --index-url https://download.pytorch.org/whl/xpu

# Explicitly pin modern PyYAML to bypass legacy Cython compilation errors on newer Python runtimes (like 3.14)
pyyaml>=6.0.2
