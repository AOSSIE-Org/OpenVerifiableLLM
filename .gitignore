detgpt-env/
env/
venv/


__pycache__/
*.pyc
build/
dist/
*.egg-info/
.ovllm-cache/


.vscode/
.idea/


*.pt
*.pth
*.jsonl
*.sig
eval_manifest.json
pipeline_manifest.json
mid_checkpoint.safetensors
mid_checkpoint.merkle.json

# Python venvs
.venv/

# ed25519: keep the PUBLIC key (so anyone can verify); never commit the private key
keys/*.key

# generated experiment outputs and swept artifacts
results/
artifacts/
runs/

# session/context scratch — never commit
repo_prompt.md

# build and packaging output
dist/
*.egg-info/

# local model/dataset download cache
.ovllm-cache/

# publish smoke-test scratch output
openverifiable-smoke/

# downloaded corpora (large); keep only the bundled offline sample
data/*.txt
data/*.zip
data/*.tar.gz
data/cifar-10-batches-py/
!data/shakespeare_sample.txt

# Local smoke tests and temp files
openverifiable-smoke/

# Active local transfer logs; immutable receipts are retained separately.
.ovllm-cache-acquisition.stderr.log

# Private operator and AI coordination material: retain locally, never publish.
/AUTONOMOUS_START.md
/PROJECT_GOAL.md
/project/goal_state.json
/project/RESUME.md
/project/LIVE_HANDOFF_INTEGRATION.md
/project/PRODUCTION_INTEGRATION.md
/project/RELEASE_INTEGRATION.md
/project/evidence/**/advisory/
/project/evidence/**/*advisory*
/project/evidence/**/usage-monitor*
/project/evidence/rtx5090-steering-20260919/
/project/evidence/provider-recovery-window-20260919/

# New operational work stays local; reviewed technical reports have a separate root.
/project/evidence/
