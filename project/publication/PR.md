The existing artifact verifier and sampled audits do not provide the full reconstruction and continuous replay required by the Wikipedia project goal. This adds a separate fail-closed pipeline whose synthetic fixture rebuilds raw inputs, regenerates initialization, replays both training phases, checks safe checkpoints, and verifies exported model/tokenizer/configuration and inference identity.

The change includes resumable checksum-verified Wikipedia acquisition; complete OASST selection and exclusion accounting; a full-data preparation driver with pinned environments and raw/metadata/receipt parent checks; exact GitHub OIDC/Sigstore identity and inclusion verification; controlled append-only source signing with public archive/upstream metadata checks; an inventory-addressed raw uploader and complete anonymous-download verifier; and conservative cost forecasting that reserves full replay and export funds. Production training admission remains closed. Actual source commitment publication, provider-side cost guards, GPU exactness, full corpus preparation and both actual model releases remain pending. No G01–G10 completion is claimed.

Validation: 151 local tests pass; three legacy GPU-dependent tests are skipped. Synthetic full preparation also reconstructs identically in a fresh hash-locked environment. Native advisory counterexamples were tested and dispositioned for the fixture and publisher-identity components; this is ordinary advisory, not independent training verification. The source-gate advisory attempt was provider-rate-limited and supplies no review result. Seven real-bundle tampering cases were rejected. The public synthetic fixture and hardened identity smoke were separately downloaded anonymously and verified. Legacy tests preserve the existing repository public key.

Public evidence: https://huggingface.co/datasets/AOSSIE/openverifiable-enwiki-20260901-20260918-r1-evidence

The full raw Wikipedia transfer has completed; complete checksum/decompression verification is running locally. No RunPod resource has been created; project RunPod spend is $0. Provider credentials/guards are required before any rental.
