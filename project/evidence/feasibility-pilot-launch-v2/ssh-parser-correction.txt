ssh-keyscan succeeded but emitted a banner comment plus the one key on stdout; prior exact-one-line assertion incorrectly rejected it. Readinessv1 retains actual response. New driver permits comment lines and validates exactly one endpoint-matching ed25519 key with correct51-byte SSH structure; existing operator-pinned TOFU and every subsequent validator unchanged. No provisioning retry or deadline change.
