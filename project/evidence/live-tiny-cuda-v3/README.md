# Third bounded CUDA fixture attempt

The full public runtime setup passed in 74 operator-observed seconds. The actual
CUDA fixture then failed with the retained error in `checkpoint.json`: disabled
split-K requires cuBLASLt, while this attempt had not selected that backend.
No completed numerical record or replay exists for this attempt.

The finite coordinator automatically exported all selected files and completed its
failure report. Controller and watchdog both confirmed pod `juxoj79yxzv4w1` absent;
a subsequent provider account read showed zero pods, volumes and hourly charge.
Automatic provider termination remains unverified. Billing attribution is pending,
so the full reserved ceiling remains in the aggregate budget.

`retained-exports.tar.gz` preserves 2,209 selected regular files (64,226,822 bytes),
including logs, complete setup/launch audits and the fresh generated bytecode cache.
The sorted inventory binds every retained byte. All archive members were extracted
into a fresh directory and completely rehashed. The archive is evidence, not an
executable model or an instruction to run its bytecode. Original selected exports
remain locally retained as well.

The first local checkpoint-packaging invocation stopped when strict canonical JSON
parsing rejected the setup helper's newline-terminated report. The exact report was
retained; the finishing invocation parsed it with the explicit noncanonical input
option, reverified the complete archive reconstruction and wrote the checkpoint.
No pod execution or numerical result was retried or relabeled by this repair.
