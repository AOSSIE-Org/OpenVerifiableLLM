The actual v3 CUDA run rejected disabled split-K with the implicit cuBLAS backend. Select cuBLASLt explicitly before precision flags and include its actual readback in every profile check. Preserve IEEE/reduction/split-K/algorithm constraints and require real fresh-process exact CUDA replay. CPU-only tests previously missed the compiled-backend requirement; test actual pinned CUDA setters on host plus CPU doubles honestly labeled. No numerical acceptance from the host check. Potential remaining issue: unsupported operations can fall back, so backend preference is not exclusive dispatch proof.
